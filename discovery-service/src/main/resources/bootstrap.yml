spring:
  application:
    name: discovery-service
  profiles:
    active: prod     # You can set this here as well, though command line will override
  config:
    import: "configserver:http://localhost:8888"
  cloud:
    config:
      username: configUser
      password: configPassword
      fail-fast: true